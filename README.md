## Getting start

Run docker container (Postgres image and RabbitMQ image )
```docker compose up -d```

Run discovery server
Run identity service
Run api gateway

Run health API in health-login-refreshToken.http

Run register in Register.http

Run Login and refresh token for testing
